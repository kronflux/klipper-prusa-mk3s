[gcode_macro M486]
description: Clear pause state (Marlin)
gcode:
    # This macro assumes M486 is intended to clear a paused state or reset similar conditions.
    CLEAR_PAUSE
    {action_respond_info("Pause state cleared.")}
