[gcode_macro M117]
description: Display a message (simulates Marlin's M117)
gcode:
    {% if 'MSG' in params %}
        {action_respond_info(params.MSG)}
    {% else %}
        {action_respond_info("M117 command received without message.")}
    {% endif %}
